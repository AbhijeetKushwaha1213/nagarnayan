# ─────────────────────────────────────────────────────────────────────────────
# Nagar Nayan Backend — Python Dependencies
#
# NOTE on Python 3.14:
#   asyncpg and psycopg2-binary require compilation on Python 3.14.
#   Inside Docker (Python 3.11) they install from pre-built wheels instantly.
#   For local development without Docker, DB-touching tests are skipped.
# ─────────────────────────────────────────────────────────────────────────────

# Web framework & server
fastapi>=0.115.0,<0.116
uvicorn[standard]>=0.32.0,<0.34

# Configuration
pydantic>=2.10.0,<3
pydantic-settings>=2.6.0,<3

# Database ORM + migrations
sqlalchemy>=2.0.0,<3
alembic>=1.14.0,<2
geoalchemy2>=0.14.0

# PostgreSQL async driver (used by the app at runtime)
asyncpg>=0.29.0

# PostgreSQL sync driver (used by Alembic migration runner only)
psycopg2-binary>=2.9.0

# Testing
pytest>=8.3.0,<9
pytest-asyncio>=0.24.0,<1
httpx>=0.28.0,<1        # required by FastAPI TestClient
anyio>=4.7.0,<5

# Development
python-dotenv>=1.0.0,<2
